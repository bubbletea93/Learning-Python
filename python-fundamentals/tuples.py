# Type of data structure
# It's like a container where we can store different values
# Tuples are immutable meaning it can't be changed or modified.
coordinates = (4, 5)

# Accessing tuple values
print(coordinates[0])
print(coordinates[1])

# Tuple list
coordinatesList = [(1, 2), (3, 4), (5, 6)]
print(coordinatesList)